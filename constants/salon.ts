// Shared salon UI tokens — matches app/(tabs)/index.tsx design language.

export const SalonColors = {
  background: "#f5f5f5",
  card: "#fff",
  textPrimary: "#111",
  textSecondary: "#333",
  textMuted: "#999",
  border: "#ccc",
  borderLight: "#e0e0e0",
  primary: "#000",
  primaryText: "#fff",
  success: "#28a745",
  danger: "#d9534f",
  disabled: "#999",
} as const;

export const SalonSpacing = {
  screenPadding: 20,
  screenTop: 60,
  cardRadius: 16,
  inputRadius: 8,
  buttonRadius: 10,
} as const;
